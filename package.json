{
  "name": "schuteiradisc",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sala única de áudio e compartilhamento de tela, sem servidor",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  },
  "dependencies": {
    "trystero": "^0.25.3"
  }
}
